// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2025, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/constraint;
import ballerina/http;

# Represents the Queries record for the operation: findPeople
public type FindPeopleQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # The term to search for
    string term;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type SharedLinkPagingList record {
    SharedLinkEntry[] entries;
    Pagination pagination;
};

public type Rendition record {
    string id?;
    ContentInfo content?;
    "CREATED"|"NOT_CREATED" status?;
};

public type Node record {
    boolean isLink?;
    boolean isFile;
    UserInfo createdByUser;
    string modifiedAt;
    string nodeType;
    string parentId?;
    ContentInfo content?;
    string[] aspectNames?;
    string createdAt;
    PathInfo path?;
    boolean isFolder;
    PermissionsInfo permissions?;
    boolean isLocked = false;
    UserInfo modifiedByUser;
    # The name must not contain spaces or the following special characters: * " < > \ / ? : and |.
    # The character . must not be used at the end of the name
    string name;
    Definition definition?;
    string id;
    record {} properties?;
    string[] allowableOperations?;
    boolean isFavorite?;
};

public type NodeBodyUpdate record {
    string[] aspectNames?;
    PermissionsBody permissions?;
    # The name must not contain spaces or the following special characters: * " < > \ / ? : and |.
    # The character . must not be used at the end of the name
    string name?;
    string nodeType?;
    record {|string...;|} properties?;
};

public type SiteGroupPaging record {
    SiteGroupPagingList list;
};

# Activities describe any past activity in a site,
# for example creating an item of content, commenting on a node,
# liking an item of content
public type Activity record {
    # The date time at which the activity was performed
    string postedAt?;
    # The feed on which this activity was posted
    string feedPersonId;
    # The id of the person who performed the activity
    string postPersonId;
    # The unique id of the site on which the activity was performed
    string siteId?;
    # An object summarizing the activity
    record {|string...;|} activitySummary?;
    # The unique id of the activity
    int id;
    # The type of the activity posted
    string activityType;
};

public type SiteContainerPagingList record {
    SiteContainerEntry[] entries;
    Pagination pagination;
};

# Represents the Queries record for the operation: getFavorite
public type GetFavoriteQueries record {
    # Returns additional information about favorites, the following optional fields can be requested:
    # * path (note, this only applies to files and folders)
    # * properties
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: copyNode
public type CopyNodeQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type SiteMembershipRequestBodyCreate record {
    # Optional client name used when sending an email to the end user, defaults to "share" if not provided.
    # **Note:** The client must be registered before this API can send an email.
    # **Note:** This is available in Alfresco 7.0.0 and newer versions
    string 'client?;
    string id;
    string message?;
    string title?;
};

public type GroupPagingList record {
    GroupEntry[] entries?;
    Pagination pagination?;
};

# Represents the Queries record for the operation: listSourceAssociations
public type ListSourceAssociationsQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * aspectNames
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * properties
    string[] include?;
    # Optionally filter the list by **assocType**. Here's an example:
    # 
    # *   ```where=(assocType='my:specialAssocType')```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Headers record for the operation: getDeletedNodeContent
public type GetDeletedNodeContentHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
    # The Range header indicates the part of a document that the server should return.
    # Single part request supported, for example: bytes=1-10
    @http:Header {name: "Range"}
    string range?;
};

public type ActivityPagingList record {
    ActivityEntry[] entries;
    Pagination pagination;
};

# Represents the Queries record for the operation: unlockNode
public type UnlockNodeQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: listSecondaryChildren
public type ListSecondaryChildrenQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * aspectNames
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * properties
    string[] include?;
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Also include **source** (in addition to **entries**) with folder information on **nodeId**
    boolean includeSource?;
    # Optionally filter the list by assocType. Here's an example:
    # 
    # *   ```where=(assocType='my:specialAssocType')```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type SiteRolePaging record {
    SiteRolePagingList list;
};

# Represents the Queries record for the operation: updateAuditApp
public type UpdateAuditAppQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type PasswordResetBody record {
    # the new password
    string password;
    # the workflow id provided in the reset password email
    string id;
    # the workflow key provided in the reset password email
    string 'key;
};

public type SiteMembershipRequest record {
    string createdAt;
    Site site;
    string id;
    string message?;
};

# Represents the Queries record for the operation: getSiteMembershipRequests
public type GetSiteMembershipRequestsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A string to restrict the returned objects by using a predicate
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Provides a set of configurations for controlling the behaviours when communicating with a remote HTTP endpoint.
@display {label: "Connection Config"}
public type ConnectionConfig record {|
    # Configurations related to client authentication
    http:CredentialsConfig auth;
    # The HTTP version understood by the client
    http:HttpVersion httpVersion = http:HTTP_2_0;
    # Configurations related to HTTP/1.x protocol
    http:ClientHttp1Settings http1Settings = {};
    # Configurations related to HTTP/2 protocol
    http:ClientHttp2Settings http2Settings = {};
    # The maximum time to wait (in seconds) for a response before closing the connection
    decimal timeout = 30;
    # The choice of setting `forwarded`/`x-forwarded` header
    string forwarded = "disable";
    # Configurations associated with Redirection
    http:FollowRedirects followRedirects?;
    # Configurations associated with request pooling
    http:PoolConfiguration poolConfig?;
    # HTTP caching related configurations
    http:CacheConfig cache = {};
    # Specifies the way of handling compression (`accept-encoding`) header
    http:Compression compression = http:COMPRESSION_AUTO;
    # Configurations associated with the behaviour of the Circuit Breaker
    http:CircuitBreakerConfig circuitBreaker?;
    # Configurations associated with retrying
    http:RetryConfig retryConfig?;
    # Configurations associated with cookies
    http:CookieConfig cookieConfig?;
    # Configurations associated with inbound response size limits
    http:ResponseLimitConfigs responseLimits = {};
    # SSL/TLS-related options
    http:ClientSecureSocket secureSocket?;
    # Proxy server related options
    http:ProxyConfig proxy?;
    # Provides settings related to client socket configuration
    http:ClientSocketConfig socketConfig = {};
    # Enables the inbound payload validation functionality which provided by the constraint package. Enabled by default
    boolean validation = true;
    # Enables relaxed data binding on the client side. When enabled, `nil` values are treated as optional, 
    # and absent fields are handled as `nilable` types. Enabled by default.
    boolean laxDataBinding = true;
|};

# Represents the Queries record for the operation: getPreference
public type GetPreferenceQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type PreferencePaging record {
    PreferencePagingList list;
};

# Represents the Queries record for the operation: listPeople
public type ListPeopleQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the person. The following optional fields can be requested:
    # * properties
    # * aspectNames
    # * capabilities
    string[] include?;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: listTags
public type ListTagsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the tag. The following optional fields can be requested:
    # * count
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type SiteMembershipBodyUpdate record {
    "SiteConsumer"|"SiteCollaborator"|"SiteContributor"|"SiteManager" role;
};

# Represents the Queries record for the operation: lockNode
public type LockNodeQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Headers record for the operation: getSharedLinkContent
public type GetSharedLinkContentHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
    # The Range header indicates the part of a document that the server should return.
    # Single part request supported, for example: bytes=1-10
    @http:Header {name: "Range"}
    string range?;
};

public type NodeBodyCopy record {
    string targetParentId;
    # The name must not contain spaces or the following special characters: * " < > \ / ? : and |.
    # The character . must not be used at the end of the name
    string name?;
};

# Represents the Queries record for the operation: listNetworksForPerson
public type ListNetworksForPersonQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: getSharedLinkContent
public type GetSharedLinkContentQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
};

public type RatingPagingList record {
    RatingEntry[] entries;
    Pagination pagination;
};

public type SiteMembershipRequestWithPerson record {
    string createdAt;
    Site site;
    Person person;
    string id;
    string message?;
};

# Represents the Queries record for the operation: listComments
public type ListCommentsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type FavoriteSite record {
    string id;
};

# Represents the Queries record for the operation: listPreferences
public type ListPreferencesQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: getAvatarImage
public type GetAvatarImageQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
    # If **true** and there is no avatar for this **personId**
    # then the placeholder image is returned, rather than a 404 response
    boolean placeholder = true;
};

# Represents the Queries record for the operation: createSiteMembershipRequestForPerson
public type CreateSiteMembershipRequestForPersonQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type Group record {
    boolean isRoot = true;
    string displayName;
    string[] parentIds?;
    string id;
    string[] zones?;
};

public type SiteMembershipRequestWithPersonEntry record {
    SiteMembershipRequestWithPerson entry;
};

public type GroupMember record {
    string displayName;
    string id;
    "GROUP"|"PERSON" memberType;
};

# Represents the Queries record for the operation: deleteGroup
public type DeleteGroupQueries record {
    # If **true** then the delete will be applied in cascade to sub-groups
    boolean cascade = false;
};

# Represents the Queries record for the operation: deleteAuditEntriesForAuditApp
public type DeleteAuditEntriesForAuditAppQueries record {
    # Audit entries to permanently delete for an audit application, given an inclusive time period or range of ids. For example:
    # 
    # *   ```where=(createdAt BETWEEN ('2017-06-02T12:13:51.593+01:00' , '2017-06-04T10:05:16.536+01:00')```
    # *   ```where=(id BETWEEN ('1234', '4321')```
    string 'where;
};

public type CommentBody record {
    string content;
};

# Represents the Queries record for the operation: findSites
public type FindSitesQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # The term to search for
    string term;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type ProbeEntry record {
    ProbeEntryEntry entry;
};

# Represents the Queries record for the operation: getDeletedNode
public type GetDeletedNodeQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
};

# Represents the Queries record for the operation: createGroupMembership
public type CreateGroupMembershipQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type ProbeEntryEntry record {
    string message;
};

# Represents the Queries record for the operation: listParents
public type ListParentsQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * aspectNames
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * properties
    string[] include?;
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Also include **source** (in addition to **entries**) with folder information on **nodeId**
    boolean includeSource?;
    # Optionally filter the list by **assocType** and/or **isPrimary**. Here are some example filters:
    # 
    # *   ```where=(assocType='my:specialAssocType')```
    # 
    # *   ```where=(isPrimary=true)```
    # 
    # *   ```where=(isPrimary=false and assocType='my:specialAssocType')```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type GroupMembershipBodyCreate record {
    string id;
    "GROUP"|"PERSON" memberType;
};

# Represents the Queries record for the operation: moveNode
public type MoveNodeQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type ClientBody record {
    # the client name
    string 'client;
};

public type Association record {
    string targetId;
    string assocType;
};

# Represents the Queries record for the operation: createRating
public type CreateRatingQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: updateSiteMembership
public type UpdateSiteMembershipQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: restoreDeletedNode
public type RestoreDeletedNodeQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type Download record {
    # number of files added so far in the zip
    int filesAdded?;
    # number of bytes added so far in the zip
    int bytesAdded?;
    # the total number of bytes to be added in the zip
    int totalBytes?;
    # the id of the download node
    string id?;
    # the total number of files to be added in the zip
    int totalFiles?;
    # the current status of the download node creation
    "PENDING"|"CANCELLED"|"IN_PROGRESS"|"DONE"|"MAX_CONTENT_SIZE_EXCEEDED" status = "PENDING";
};

public type AuditAppEntry record {
    AuditApp entry?;
};

public type AuditApp record {
    int maxEntryId?;
    boolean isEnabled = true;
    string name?;
    int minEntryId?;
    string id;
};

public type NodeBodyLock record {
    @constraint:Int {minValue: 0}
    int timeToExpire?;
    "PERSISTENT"|"EPHEMERAL" lifetime = "PERSISTENT";
    "ALLOW_OWNER_CHANGES"|"FULL" 'type = "ALLOW_OWNER_CHANGES";
};

public type NodePagingList record {
    NodeEntry[] entries?;
    Pagination pagination?;
    Node 'source?;
};

public type SiteMembershipRequestWithPersonPagingList record {
    SiteMembershipRequestWithPersonEntry[] entries;
    Pagination pagination;
};

public type AssociationInfo record {
    string assocType;
};

public type SiteRole record {
    Site site;
    "SiteConsumer"|"SiteCollaborator"|"SiteContributor"|"SiteManager" role;
    string guid;
    string id;
};

# Represents the Queries record for the operation: updateTag
public type UpdateTagQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: updatePerson
public type UpdatePersonQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type AuditEntryPaging record {
    AuditEntryPagingList list?;
};

public type SharedLink record {
    # The allowable operations for the content node being shared
    string[] allowableOperationsOnTarget?;
    string modifiedAt?;
    string description?;
    string title?;
    string expiresAt?;
    ContentInfo content?;
    string[] aspectNames?;
    PathInfo path?;
    UserInfo modifiedByUser?;
    # The name must not contain spaces or the following special characters: * " < > \ / ? : and |.
    # The character . must not be used at the end of the name
    string name?;
    string id?;
    string nodeId?;
    UserInfo sharedByUser?;
    # The allowable operations for the Quickshare link itself. See allowableOperationsOnTarget for the
    # allowable operations pertaining to the linked content node
    string[] allowableOperations?;
    # A subset of the target node's properties, system properties and properties already available in the SharedLink are excluded
    record {} properties?;
    boolean isFavorite?;
};

public type FavoritePagingList record {
    FavoriteEntry[] entries;
    Pagination pagination;
};

# Represents the Queries record for the operation: getArchivedNodeRenditionContent
public type GetArchivedNodeRenditionContentQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
    # If **true** and there is no rendition for this **nodeId** and **renditionId**,
    # then the placeholder image for the mime type of this rendition is returned, rather
    # than a 404 response
    boolean placeholder = false;
};

public type SiteGroupPagingList record {
    SiteGroupEntry[] entries;
    Pagination pagination;
};

# Represents the Queries record for the operation: listAuditEntriesForNode
public type ListAuditEntriesForNodeQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the audit entry. The following optional fields can be requested:
    # * values
    string[] include?;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # Optionally filter the list. Here are some examples:
    # 
    # *   ```where=(createdByUser='-me-')```
    # 
    # *   ```where=(createdAt BETWEEN ('2017-06-02T12:13:51.593+01:00' , '2017-06-04T10:05:16.536+01:00')```
    # 
    # *   ```where=(createdByUser='jbloggs' and createdAt BETWEEN ('2017-06-02T12:13:51.593+01:00' , '2017-06-04T10:05:16.536+01:00')```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: listSiteMembershipsForPerson
public type ListSiteMembershipsForPersonQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A string to restrict the returned objects by using a predicate
    string 'where?;
    # Use the relations parameter to include one or more related entities in a single response
    string[] relations?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type RenditionBodyCreate record {
    string id;
};

# Represents the Queries record for the operation: listVersionRenditions
public type ListVersionRenditionsQueries record {
    # A string to restrict the returned objects by using a predicate
    string 'where?;
};

public type GroupMemberEntry record {
    GroupMember entry;
};

public type GroupEntry record {
    Group entry;
};

public type SiteMembershipRequestWithPersonPaging record {
    SiteMembershipRequestWithPersonPagingList list;
};

public type NodeChildAssociationAllOf2 record {
    ChildAssociationInfo association?;
};

public type NodeAssociation record {
    *Node;
    *NodeAssociationAllOf2;
};

public type ActivityPaging record {
    ActivityPagingList list;
};

public type PersonNetworkPaging record {
    PersonNetworkPagingList list;
};

public type Constraint record {
    # the human-readable constraint description
    string description?;
    string id;
    # the type of the constraint
    string 'type?;
    # the human-readable constraint title
    string title?;
    record {|record {}...;|} parameters?;
};

# Represents the Queries record for the operation: listDeletedNodeRenditions
public type ListDeletedNodeRenditionsQueries record {
    # A string to restrict the returned objects by using a predicate
    string 'where?;
};

# Represents the Queries record for the operation: getSharedLink
public type GetSharedLinkQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type SharedLinkEntry record {
    SharedLink entry;
};

public type ActionDefinition record {
    # QNames of the types this action applies to
    string[] applicableTypes;
    ActionParameterDefinition[] parameterDefinitions?;
    # name of the action definition, e.g. "move"
    string name?;
    # whether the basic action definition supports action tracking or not
    boolean trackStatus;
    # describes the action definition, e.g. "This will move the matched item to another space."
    string description?;
    # Identifier of the action definition — used for example when executing an action
    string id;
    # title of the action definition, e.g. "Move"
    string title?;
};

# Represents the Queries record for the operation: listNodeChildren
public type ListNodeChildrenQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * aspectNames
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * properties
    # * permissions
    string[] include?;
    # Also include **source** in addition to **entries** with folder information on the parent node – either the specified parent **nodeId**, or as resolved by **relativePath**
    boolean includeSource?;
    # Return information on children in the folder resolved by this path. The path is relative to **nodeId**
    string relativePath?;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # Optionally filter the list. Here are some examples:
    # 
    # *   ```where=(isFolder=true)```
    # 
    # *   ```where=(isFile=true)```
    # 
    # *   ```where=(nodeType='my:specialNodeType')```
    # 
    # *   ```where=(nodeType='my:specialNodeType INCLUDESUBTYPES')```
    # 
    # *   ```where=(isPrimary=true)```
    # 
    # *   ```where=(assocType='my:specialAssocType')```
    # 
    # *   ```where=(isPrimary=false and assocType='my:specialAssocType')```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type NodeChildAssociationPagingList record {
    NodeChildAssociationEntry[] entries?;
    Pagination pagination?;
    Node 'source?;
};

public type ContentInfo record {
    int sizeInBytes?;
    string mimeTypeName?;
    string mimeType;
    string encoding?;
};

public type Version record {
    string[] aspectNames?;
    boolean isFolder;
    boolean isFile;
    string modifiedAt;
    UserInfo modifiedByUser;
    string versionComment?;
    # The name must not contain spaces or the following special characters: * " < > \ / ? : and |.
    # The character . must not be used at the end of the name
    string name;
    string id;
    string nodeType;
    ContentInfo content?;
    record {} properties?;
};

public type RatingEntry record {
    # A person can rate an item of content by liking it. They can also remove
    # their like of an item of content. API methods exist to get a list of
    # ratings and to add a new rating
    Rating entry;
};

public type NodeBodyMove record {
    string targetParentId;
    # The name must not contain spaces or the following special characters: * " < > \ / ? : and |.
    # The character . must not be used at the end of the name
    string name?;
};

# Represents the Queries record for the operation: getAuditApp
public type GetAuditAppQueries record {
    # Also include the current minimum and/or maximum audit entry ids for the application. The following optional fields can be requested:
    # * max
    # * min
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# A network is the group of users and sites that belong to an organization.
# Networks are organized by email domain. When a user signs up for an
# Alfresco account , their email domain becomes their Home Network
public type PersonNetwork record {
    string createdAt?;
    boolean paidNetwork?;
    "Free"|"Standard"|"Enterprise" subscriptionLevel?;
    NetworkQuota[] quotas?;
    boolean isEnabled;
    # This network's unique id
    string id;
    # Is this the home network?
    boolean homeNetwork?;
};

# Represents the Queries record for the operation: createNode
public type CreateNodeQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
    # If true, then created node will be versioned. If false, then created node will be unversioned and auto-versioning disabled
    boolean versioningEnabled?;
    # If true, then  a name clash will cause an attempt to auto rename by finding a unique name using an integer suffix
    boolean autoRename?;
    # If true, then created node will be version *1.0 MAJOR*. If false, then created node will be version *0.1 MINOR*
    boolean majorVersion?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type VersionEntry record {
    Version entry?;
};

# Represents the Queries record for the operation: createSiteMembership
public type CreateSiteMembershipQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type GroupMemberPaging record {
    GroupMemberPagingList list?;
};

public type SharedLinkPaging record {
    SharedLinkPagingList list;
};

public type ActionDefinitionList record {
    ActionDefinitionListList list?;
};

public type NodePaging record {
    NodePagingList list?;
};

public type RenditionPagingList record {
    RenditionEntry[] entries?;
    Pagination pagination?;
};

public type SiteMembershipBodyCreate record {
    "SiteConsumer"|"SiteCollaborator"|"SiteContributor"|"SiteManager" role;
    string id;
};

# Represents the Queries record for the operation: getTag
public type GetTagQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type SiteRolePagingList record {
    SiteRoleEntry[] entries;
    Pagination pagination;
};

public type NodeEntry record {
    Node entry;
};

public type CommentEntry record {
    Comment entry;
};

public type GroupPaging record {
    GroupPagingList list?;
};

public type PermissionElement record {
    string authorityId?;
    string name?;
    "ALLOWED"|"DENIED" accessStatus = "ALLOWED";
};

# Represents the Queries record for the operation: listAuditApps
public type ListAuditAppsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: getNodeContent
public type GetNodeContentQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
};

public type PreferencePagingList record {
    PreferenceEntry[] entries;
    Pagination pagination;
};

# Represents the Queries record for the operation: getSiteMembershipRequestForPerson
public type GetSiteMembershipRequestForPersonQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type GroupMemberPagingList record {
    GroupMemberEntry[] entries?;
    Pagination pagination?;
};

# Represents the Headers record for the operation: getNodeContent
public type GetNodeContentHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
    # The Range header indicates the part of a document that the server should return.
    # Single part request supported, for example: bytes=1-10
    @http:Header {name: "Range"}
    string range?;
};

public type DownloadEntry record {
    Download entry;
};

public type GroupBodyUpdate record {
    string displayName;
};

# Represents the Queries record for the operation: createSite
public type CreateSiteQueries record {
    # Flag to indicate whether the site should not be added to the user's site favorites
    boolean skipAddToFavorites = false;
    # Flag to indicate whether the Share-specific (surf) configuration files for the site should not be created
    boolean skipConfiguration = false;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type ChildAssociation record {
    string assocType;
    string childId;
};

public type AuditEntryEntry record {
    AuditEntry entry?;
};

public type RatingAggregate record {
    int average?;
    int numberOfRatings;
};

public type NodeAssociationEntry record {
    NodeAssociation entry;
};

# Represents the Headers record for the operation: getAvatarImage
public type GetAvatarImageHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
};

# A person can rate an item of content by liking it. They can also remove
# their like of an item of content. API methods exist to get a list of
# ratings and to add a new rating
public type Rating record {
    # The rating. The type is specific to the rating scheme, boolean for the likes and an integer for the fiveStar
    string myRating?;
    string ratedAt?;
    string id;
    RatingAggregate aggregate;
};

# Represents the Queries record for the operation: createFavorite
public type CreateFavoriteQueries record {
    # Returns additional information about favorites, the following optional fields can be requested:
    # * path (note, this only applies to files and folders)
    # * properties
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type PermissionsBody record {
    boolean isInheritanceEnabled?;
    PermissionElement[] locallySet?;
};

public type UserInfo record {
    string displayName;
    string id;
};

public type SiteGroupEntry record {
    SiteGroup entry;
};

public type SharedLinkBodyEmail record {
    string 'client?;
    string[] recipientEmails?;
    string message?;
    string locale?;
};

public type NodeAssociationPagingList record {
    NodeAssociationEntry[] entries?;
    Pagination pagination?;
    Node 'source?;
};

# Represents the Queries record for the operation: createTagForNode
public type CreateTagForNodeQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type TagPaging record {
    TagPagingList list;
};

public type NodeAssociationAllOf2 record {
    AssociationInfo association?;
};

public type AssociationEntry record {
    Association entry;
};

# Limits and usage of each quota. A network will have quotas for File space,
# the number of sites in the network, the number of people in the network,
# and the number of network administrators
public type NetworkQuota record {
    int usage;
    int 'limit;
    string id;
};

public type SiteMemberPagingList record {
    SiteMemberEntry[] entries;
    Pagination pagination;
};

# Represents the Queries record for the operation: getDeletedNodeContent
public type GetDeletedNodeContentQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
};

public type TagPagingList record {
    TagEntry[] entries;
    Pagination pagination;
};

public type SitePagingList record {
    SiteEntry[] entries;
    Pagination pagination;
};

public type TagBody record {
    string tag;
};

# Represents the Queries record for the operation: getVersionContent
public type GetVersionContentQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
};

public type PersonPagingList record {
    PersonEntry[] entries?;
    Pagination pagination?;
};

# Represents the Queries record for the operation: listActions
public type ListActionsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type ActivityEntry record {
    # Activities describe any past activity in a site,
    # for example creating an item of content, commenting on a node,
    # liking an item of content
    Activity entry;
};

public type ActionExecResultEntry record {
    ActionExecResult entry;
};

# Represents the Queries record for the operation: nodeActions
public type NodeActionsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type ChildAssociationBody record {
    string assocType;
    string childId;
};

# Represents the Queries record for the operation: getNetwork
public type GetNetworkQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type Capabilities record {
    boolean isMutable?;
    boolean isGuest?;
    boolean isAdmin?;
};

public type NodeChildAssociationEntry record {
    NodeChildAssociation entry;
};

# Represents the Queries record for the operation: listSiteContainers
public type ListSiteContainersQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: createSiteFavorite
public type CreateSiteFavoriteQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: listSites
public type ListSitesQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A string to restrict the returned objects by using a predicate
    string 'where?;
    # Use the relations parameter to include one or more related entities in a single response
    string[] relations?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: getNode
public type GetNodeQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
    # A path relative to the **nodeId**. If you set this,
    # information is returned on the node resolved by this path
    string relativePath?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type NodeAssociationPaging record {
    NodeAssociationPagingList list?;
};

public type FavoriteSiteEntry record {
    FavoriteSite entry;
};

# Represents the Queries record for the operation: listRatings
public type ListRatingsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: updateNodeContent
public type UpdateNodeContentQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
    # Optional new name. This should include the file extension.
    # The name must not contain spaces or the following special characters: * " < > \ / ? : and |.
    # The character `.` must not be used at the end of the name
    string name?;
    # Add a version comment which will appear in version history.
    # Setting this parameter also enables versioning of this node, if it is not already versioned
    string comment?;
    # If **true**, create a major version.
    # Setting this parameter also enables versioning of this node, if it is not already versioned
    boolean majorVersion = false;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: createSecondaryChildAssociation
public type CreateSecondaryChildAssociationQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type AuditEntryPagingList record {
    AuditEntryEntry[] entries?;
    Pagination pagination?;
};

public type SiteMember record {
    "SiteConsumer"|"SiteCollaborator"|"SiteContributor"|"SiteManager" role;
    Person person;
    string id;
    boolean isMemberOfGroup?;
};

# Represents the Queries record for the operation: listSiteGroups
public type ListSiteGroupsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type ActionDefinitionListList record {
    ActionDefinition[] entries?;
    Pagination pagination?;
};

public type SiteGroup record {
    "SiteConsumer"|"SiteCollaborator"|"SiteContributor"|"SiteManager" role;
    string id;
    GroupMember group;
};

public type SiteRoleEntry record {
    SiteRole entry;
};

# Represents the Queries record for the operation: listGroups
public type ListGroupsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the group. The following optional fields can be requested:
    # * parentIds
    # * zones
    string[] include?;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A string to restrict the returned objects by using a predicate
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type SiteMembershipRequestEntry record {
    SiteMembershipRequest entry;
};

public type RenditionEntry record {
    Rendition entry;
};

public type FavoriteBodyCreate record {
    record {} target;
};

# Represents the Queries record for the operation: listActivitiesForPerson
public type ListActivitiesForPersonQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Include only activity feed entries relating to this site
    string siteId?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
    # A filter to include the user's activities only `me`, other user's activities only `others`'
    string who?;
};

public type PathElement record {
    string[] aspectNames?;
    string name?;
    string id?;
    string nodeType?;
};

# Represents the Queries record for the operation: getDownload
public type GetDownloadQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: getSiteMembership
public type GetSiteMembershipQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type FavoriteSiteBodyCreate record {
    string id;
};

public type SiteMemberEntry record {
    SiteMember entry;
};

public type SitePaging record {
    SitePagingList list;
};

public type SiteMembershipRejectionBody record {
    string comment?;
};

# Represents the Queries record for the operation: getSiteContainer
public type GetSiteContainerQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: revertVersion
public type RevertVersionQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type ActionBodyExec record {
    # The entity upon which to execute the action, typically a node ID or similar
    string targetId?;
    string actionDefinitionId;
    record {} params?;
};

# Represents the Queries record for the operation: createComment
public type CreateCommentQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: getSite
public type GetSiteQueries record {
    # Use the relations parameter to include one or more related entities in a single response
    string[] relations?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type GroupBodyCreate record {
    string displayName;
    string[] parentIds?;
    string id;
};

public type Definition record {
    # List of property definitions effective for this node as the result of combining the type with all aspects
    Property[] properties?;
};

public type NodeBodyCreate record {
    string[] aspectNames?;
    PermissionsBody permissions?;
    string relativePath?;
    # The name must not contain spaces or the following special characters: * " < > \ / ? : and |.
    # The character . must not be used at the end of the name
    string name;
    NodeBodyCreateAssociation association?;
    ChildAssociationBody[] secondaryChildren?;
    Definition definition?;
    string nodeType;
    AssociationBody[] targets?;
    record {} properties?;
};

# Represents the Queries record for the operation: findNodes
public type FindNodesQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * aspectNames
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * properties
    string[] include?;
    # The id of the node to start the search from.
    # 
    # Supports the aliases -my-, -root- and -shared-
    string rootNodeId?;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # The term to search for
    string term;
    # Restrict the returned results to only those of the given node type and its sub-types
    string nodeType?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type PersonNetworkEntry record {
    # A network is the group of users and sites that belong to an organization.
    # Networks are organized by email domain. When a user signs up for an
    # Alfresco account , their email domain becomes their Home Network
    PersonNetwork entry;
};

public type NodeBodyCreateAssociation record {
    string assocType?;
};

public type SharedLinkBodyCreate record {
    string nodeId;
    string expiresAt?;
};

# A specific preference
public type Preference record {
    # The unique id of the preference
    string id;
    # The value of the preference. Note that this can be of any JSON type
    string value?;
};

public type AuditEntry record {
    string createdAt;
    UserInfo createdByUser;
    record {} values?;
    string auditApplicationId;
    string id;
};

public type RenditionPaging record {
    RenditionPagingList list?;
};

public type DeletedNodeAllOf2 record {
    string archivedAt;
    UserInfo archivedByUser;
};

# Represents the Queries record for the operation: updateSiteMembershipRequestForPerson
public type UpdateSiteMembershipRequestForPersonQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type Person record {
    string googleId?;
    string lastName?;
    string userStatus?;
    Capabilities capabilities?;
    string displayName?;
    string jobTitle?;
    string statusUpdatedAt?;
    string mobile?;
    boolean emailNotificationsEnabled = true;
    string description?;
    string telephone?;
    boolean enabled = true;
    string[] aspectNames?;
    string firstName;
    string skypeId?;
    string avatarId?;
    string instantMessageId?;
    string location?;
    Company company?;
    string id;
    string email;
    record {} properties?;
};

public type SiteContainer record {
    string id;
    string folderId;
};

public type Site record {
    "SiteConsumer"|"SiteCollaborator"|"SiteContributor"|"SiteManager" role?;
    "PRIVATE"|"MODERATED"|"PUBLIC" visibility;
    string guid;
    string description?;
    string id;
    string preset?;
    string title;
};

public type SiteMembershipRequestBodyUpdate record {
    string message?;
};

# Represents the Queries record for the operation: deleteSecondaryChildAssociation
public type DeleteSecondaryChildAssociationQueries record {
    # Only delete associations of this type
    string assocType?;
};

# Represents the Queries record for the operation: listGroupMemberships
public type ListGroupMembershipsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A string to restrict the returned objects by using a predicate
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: deleteAssociation
public type DeleteAssociationQueries record {
    # Only delete associations of this type
    string assocType?;
};

public type PersonNetworkPagingList record {
    PersonNetworkEntry[] entries;
    Pagination pagination;
};

public type SiteMembershipApprovalBody record {
    string role?;
};

public type ActionDefinitionEntry record {
    ActionDefinition entry;
};

public type VersionPagingList record {
    VersionEntry[] entries?;
    Pagination pagination?;
};

public type DeletedNodesPagingList record {
    DeletedNodeEntry[] entries?;
    Pagination pagination?;
};

# Represents the Queries record for the operation: listSiteMembershipRequestsForPerson
public type ListSiteMembershipRequestsForPersonQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: createDownload
public type CreateDownloadQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type NodeChildAssociationPaging record {
    NodeChildAssociationPagingList list?;
};

public type CommentPagingList record {
    CommentEntry[] entries;
    Pagination pagination;
};

# Represents the Queries record for the operation: listVersionHistory
public type ListVersionHistoryQueries record {
    # Returns additional information about the version node. The following optional fields can be requested:
    # * properties
    # * aspectNames
    string[] include?;
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type NodeChildAssociation record {
    *Node;
    *NodeChildAssociationAllOf2;
};

public type DeletedNodeEntry record {
    DeletedNode entry?;
};

public type ChildAssociationEntry record {
    ChildAssociation entry;
};

public type DeletedNode record {
    *Node;
    *DeletedNodeAllOf2;
};

public type Property record {
    # define if the property is system maintained
    boolean isProtected?;
    # the default value
    string defaultValue?;
    # the name of the property type (e.g. d:text)
    string dataType?;
    # define if the property is multi-valued
    boolean isMultiValued?;
    # the human-readable description
    string description?;
    # define if the presence of mandatory properties is enforced
    boolean isMandatoryEnforced?;
    string id;
    # the human-readable title
    string title?;
    # list of constraints defined for the property
    Constraint[] constraints?;
    # define if the property is mandatory
    boolean isMandatory?;
};

public type SiteMembershipRequestPagingList record {
    SiteMembershipRequestEntry[] entries;
    Pagination pagination;
};

# Represents the Queries record for the operation: updateGroup
public type UpdateGroupQueries record {
    # Returns additional information about the group. The following optional fields can be requested:
    # * parentIds
    # * zones
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type AssociationBody record {
    string targetId;
    string assocType;
};

# Represents the Queries record for the operation: getRenditionContent
public type GetRenditionContentQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
    # If **true** and there is no rendition for this **nodeId** and **renditionId**,
    # then the placeholder image for the mime type of this rendition is returned, rather
    # than a 404 response
    boolean placeholder = false;
};

# Represents the Headers record for the operation: getVersionContent
public type GetVersionContentHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
    # The Range header indicates the part of a document that the server should return.
    # Single part request supported, for example: bytes=1-10
    @http:Header {name: "Range"}
    string range?;
};

# Represents the Queries record for the operation: createSiteGroupMembership
public type CreateSiteGroupMembershipQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type PersonEntry record {
    Person entry;
};

public type FavoritePaging record {
    FavoritePagingList list;
};

# Represents the Queries record for the operation: getVersionRenditionContent
public type GetVersionRenditionContentQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
    # If **true** and there is no rendition for this **nodeId** and **renditionId**,
    # then the placeholder image for the mime type of this rendition is returned, rather
    # than a 404 response
    boolean placeholder = false;
};

public type Tag record {
    decimal count?;
    string id;
    string tag;
};

public type SiteEntry record {
    Site entry;
};

# Represents the Queries record for the operation: getFavoriteSite
public type GetFavoriteSiteQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: listSiteMemberships
public type ListSiteMembershipsQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Optionally filter the list.
    # *   ```where=(isMemberOfGroup=false|true)```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: getGroup
public type GetGroupQueries record {
    # Returns additional information about the group. The following optional fields can be requested:
    # * parentIds
    # * zones
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type Company record {
    string address3?;
    string address2?;
    string address1?;
    string organization?;
    string postcode?;
    string telephone?;
    string fax?;
    string email?;
};

# Represents the Queries record for the operation: updateComment
public type UpdateCommentQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type SiteBodyCreate record {
    "PUBLIC"|"PRIVATE"|"MODERATED" visibility = "PUBLIC";
    string description?;
    string id?;
    string title;
};

# Represents the Queries record for the operation: getSharedLinkRenditionContent
public type GetSharedLinkRenditionContentQueries record {
    # **true** enables a web browser to download the file as an attachment.
    # **false** means a web browser may preview the file in a new tab or window, but not
    # download the file.
    # 
    # You can only set this parameter to **false** if the content type of the file is in the supported list;
    # for example, certain image files and PDF files.
    # 
    # If the content type is not supported for preview, then a value of **false**  is ignored, and
    # the attachment will be returned in the response
    boolean attachment = true;
};

public type Pagination record {
    # The value of the **maxItems** parameter used to generate this list.
    # If there was no **maxItems** parameter then the default value is 100
    int maxItems?;
    # A boolean value which is **true** if there are more entities in the collection
    # beyond those in this response. A true value means a request with a larger value
    # for the **skipCount** or the **maxItems** parameter will return more entities
    boolean hasMoreItems?;
    # An integer describing the total number of entities in the collection.
    # The API might not be able to determine this value,
    # in which case this property will not be present
    int totalItems?;
    # The number of objects in the entries array
    int count?;
    # An integer describing how many entities exist in the collection before
    # those included in this list. If there was no **skipCount** parameter then the
    # default value is 0
    int skipCount?;
};

# Represents the Queries record for the operation: listTagsForNode
public type ListTagsForNodeQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: updateNode
public type UpdateNodeQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * permissions
    # * definition
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type RatingBody record {
    # The rating. The type is specific to the rating scheme, boolean for the likes and an integer for the fiveStar
    string myRating;
    # The rating scheme type. Possible values are likes and fiveStar
    "likes"|"fiveStar" id = "likes";
};

public type SiteContainerPaging record {
    SiteContainerPagingList list;
};

# Represents the Queries record for the operation: createGroup
public type CreateGroupQueries record {
    # Returns additional information about the group. The following optional fields can be requested:
    # * parentIds
    # * zones
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: getRating
public type GetRatingQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type AuditBodyUpdate record {
    boolean isEnabled?;
};

# Represents the Queries record for the operation: listTargetAssociations
public type ListTargetAssociationsQueries record {
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * aspectNames
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * properties
    string[] include?;
    # Optionally filter the list by **assocType**. Here's an example:
    # 
    # *   ```where=(assocType='my:specialAssocType')```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type DeletedNodeBodyRestore record {
    string targetParentId?;
    string assocType?;
};

public type PersonPaging record {
    PersonPagingList list?;
};

public type PersonBodyCreate record {
    string googleId?;
    string lastName?;
    string userStatus?;
    string jobTitle?;
    string mobile?;
    boolean emailNotificationsEnabled = true;
    string description?;
    string telephone?;
    boolean enabled = true;
    string[] aspectNames?;
    string firstName;
    string skypeId?;
    string password;
    string instantMessageId?;
    string location?;
    Company company?;
    string id;
    string email;
    record {} properties?;
};

# Represents the Queries record for the operation: getPerson
public type GetPersonQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: listGroupMembershipsForPerson
public type ListGroupMembershipsForPersonQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the group. The following optional fields can be requested:
    # * parentIds
    # * zones
    string[] include?;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A string to restrict the returned objects by using a predicate
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Headers record for the operation: getSharedLinkRenditionContent
public type GetSharedLinkRenditionContentHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
    # The Range header indicates the part of a document that the server should return.
    # Single part request supported, for example: bytes=1-10
    @http:Header {name: "Range"}
    string range?;
};

# Represents the Queries record for the operation: listDeletedNodes
public type ListDeletedNodesQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the node. The following optional fields can be requested:
    # * allowableOperations
    # * aspectNames
    # * association
    # * isLink
    # * isFavorite
    # * isLocked
    # * path
    # * properties
    # * permissions
    string[] include?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: deleteNode
public type DeleteNodeQueries record {
    # If **true** then the node is deleted permanently, without moving to the trashcan.
    # Only the owner of the node or an admin can permanently delete the node
    boolean permanent = false;
};

public type DownloadBodyCreate record {
    string[] nodeIds;
};

public type ActionParameterDefinition record {
    string displayLabel?;
    string name?;
    string 'type?;
    boolean multiValued?;
    boolean mandatory?;
};

# Represents the Queries record for the operation: updateSite
public type UpdateSiteQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type PersonBodyUpdate record {
    string googleId?;
    string lastName?;
    string userStatus?;
    string oldPassword?;
    string jobTitle?;
    string mobile?;
    boolean emailNotificationsEnabled?;
    string description?;
    string telephone?;
    boolean enabled?;
    string[] aspectNames?;
    string firstName?;
    string skypeId?;
    string password?;
    string instantMessageId?;
    string location?;
    Company company?;
    string email?;
    record {} properties?;
};

# Represents the Queries record for the operation: createSharedLink
public type CreateSharedLinkQueries record {
    # Returns additional information about the shared link, the following optional fields can be requested:
    # * allowableOperations
    # * path
    # * properties
    # * isFavorite
    # * aspectNames
    string[] include?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type CommentPaging record {
    CommentPagingList list;
};

# Represents the Queries record for the operation: listSharedLinks
public type ListSharedLinksQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the shared link, the following optional fields can be requested:
    # * allowableOperations
    # * path
    # * properties
    # * isFavorite
    # * aspectNames
    string[] include?;
    # Optionally filter the list by "sharedByUser" userid of person who shared the link (can also use -me-)
    # 
    # *   ```where=(sharedByUser='jbloggs')```
    # 
    # *   ```where=(sharedByUser='-me-')```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type PermissionsInfo record {
    boolean isInheritanceEnabled?;
    string[] settable?;
    PermissionElement[] locallySet?;
    PermissionElement[] inherited?;
};

# A favorite describes an Alfresco entity that a person has marked as a favorite.
# The target can be a site, file or folder
public type Favorite record {
    # The guid of the object that is a favorite
    string targetGuid;
    # The time the object was made a favorite
    string createdAt?;
    # A subset of the target favorite properties, system properties and properties already available in the target are excluded
    record {} properties?;
    record {} target;
};

# Represents the Queries record for the operation: deleteSite
public type DeleteSiteQueries record {
    # Flag to indicate whether the site should be permanently deleted i.e. bypass the trashcan
    boolean permanent = false;
};

public type SiteContainerEntry record {
    SiteContainer entry;
};

# Represents the Queries record for the operation: createPerson
public type CreatePersonQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: getSiteGroupMembership
public type GetSiteGroupMembershipQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type AuditAppPaging record {
    AuditAppPagingList list?;
};

# Represents the Queries record for the operation: listRenditions
public type ListRenditionsQueries record {
    # A string to restrict the returned objects by using a predicate
    string 'where?;
};

public type SiteMemberPaging record {
    SiteMemberPagingList list;
};

public type AuditAppPagingList record {
    AuditAppEntry[] entries?;
    Pagination pagination?;
};

# Represents the Queries record for the operation: listFavoriteSitesForPerson
public type ListFavoriteSitesForPersonQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type SiteMembershipRequestPaging record {
    SiteMembershipRequestPagingList list;
};

public type PreferenceEntry record {
    # A specific preference
    Preference entry;
};

public type ActionExecResult record {
    # The unique identifier of the action pending execution
    string id;
};

# Represents the Queries record for the operation: getAuditEntry
public type GetAuditEntryQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type DeletedNodesPaging record {
    DeletedNodesPagingList list?;
};

public type Comment record {
    string createdAt;
    Person createdBy;
    boolean edited;
    string modifiedAt;
    boolean canEdit;
    Person modifiedBy;
    boolean canDelete;
    string id;
    string title;
    string content;
};

# Represents the Headers record for the operation: getRenditionContent
public type GetRenditionContentHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
    # The Range header indicates the part of a document that the server should return.
    # Single part request supported, for example: bytes=1-10
    @http:Header {name: "Range"}
    string range?;
};

# Represents the Queries record for the operation: getNetworkForPerson
public type GetNetworkForPersonQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type ChildAssociationInfo record {
    boolean isPrimary;
    string assocType;
};

# Represents the Headers record for the operation: getArchivedNodeRenditionContent
public type GetArchivedNodeRenditionContentHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
    # The Range header indicates the part of a document that the server should return.
    # Single part request supported, for example: bytes=1-10
    @http:Header {name: "Range"}
    string range?;
};

# Represents the Queries record for the operation: createAssociation
public type CreateAssociationQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

# Represents the Queries record for the operation: listAuditEntriesForAuditApp
public type ListAuditEntriesForAuditAppQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about the audit entry. The following optional fields can be requested:
    # * values
    string[] include?;
    # A boolean to control if the response provides the total numbers of items in the collection.
    # If not supplied then the default value is false
    boolean omitTotalItems = false;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # Optionally filter the list. Here are some examples:
    # 
    # *   ```where=(createdByUser='jbloggs')```
    # 
    # *   ```where=(id BETWEEN ('1234', '4321')```
    # 
    # *   ```where=(createdAt BETWEEN ('2017-06-02T12:13:51.593+01:00' , '2017-06-04T10:05:16.536+01:00')```
    # 
    # *   ```where=(createdByUser='jbloggs' and createdAt BETWEEN ('2017-06-02T12:13:51.593+01:00' , '2017-06-04T10:05:16.536+01:00')```
    # 
    # *   ```where=(valuesKey='/alfresco-access/login/user')```
    # 
    # *   ```where=(valuesKey='/alfresco-access/transaction/action' and valuesValue='DELETE')```
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

public type FavoriteEntry record {
    # A favorite describes an Alfresco entity that a person has marked as a favorite.
    # The target can be a site, file or folder
    Favorite entry;
};

public type SiteBodyUpdate record {
    "PRIVATE"|"MODERATED"|"PUBLIC" visibility?;
    string description?;
    string title?;
};

public type RevertBody record {
    string comment?;
    boolean majorVersion?;
};

# Represents the Queries record for the operation: listFavorites
public type ListFavoritesQueries record {
    # The maximum number of items to return in the list.
    # If not supplied then the default value is 100
    @constraint:Int {minValue: 1}
    int maxItems = 100;
    # Returns additional information about favorites, the following optional fields can be requested:
    # * path (note, this only applies to files and folders)
    # * properties
    string[] include?;
    # A string to control the order of the entities returned in a list. You can use the **orderBy** parameter to
    # sort the list by one or more fields.
    # 
    # Each field has a default sort order, which is normally ascending order. Read the API method implementation notes
    # above to check if any fields used in this method have a descending default search order.
    # 
    # To sort the entities in a specific order, you can use the **ASC** and **DESC** keywords for any field
    string[] orderBy?;
    # A string to restrict the returned objects by using a predicate
    string 'where?;
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
    # The number of entities that exist in the collection before those included in this list.
    # If not supplied then the default value is 0
    @constraint:Int {minValue: 0}
    int skipCount = 0;
};

# Represents the Queries record for the operation: updateSiteGroupMembership
public type UpdateSiteGroupMembershipQueries record {
    # A list of field names.
    # 
    # You can use this parameter to restrict the fields
    # returned within a response if, for example, you want to save on overall bandwidth.
    # 
    # The list applies to a returned individual
    # entity or entries within a collection.
    # 
    # If the API method also supports the **include**
    # parameter, then the fields specified in the **include**
    # parameter are returned in addition to those specified in the **fields** parameter
    string[] fields?;
};

public type VersionPaging record {
    VersionPagingList list?;
};

public type TagEntry record {
    Tag entry;
};

public type PathInfo record {
    PathElement[] elements?;
    string name?;
    boolean isComplete?;
};

public type RatingPaging record {
    RatingPagingList list;
};

# Represents the Headers record for the operation: getVersionRenditionContent
public type GetVersionRenditionContentHeaders record {
    # Only returns the content if it has been modified since the date provided.
    # Use the date format defined by HTTP. For example, `Wed, 09 Mar 2016 16:56:34 GMT`
    @http:Header {name: "If-Modified-Since"}
    string ifModifiedSince?;
    # The Range header indicates the part of a document that the server should return.
    # Single part request supported, for example: bytes=1-10
    @http:Header {name: "Range"}
    string range?;
};
